# タスクツリーへのJiraキー表示 タスクリスト

## 1. 実装

- [x] `src/views/taskTreeViewProvider.ts`の`TaskTreeItem.buildDescription`を`design.md`の実装アプローチ通りに変更する

## 2. 品質チェック

- [x] 型チェック(`npm run typecheck`)が通ることを確認
- [x] Lintが通ることを確認
- [x] 単体テスト・結合テストのコンパイルが通ることを確認(既存テストの破壊がないこと)
- [x] `.vsix`をビルドして起動確認できる状態にする

## 3. 実機確認

- [x] `jiraIssueKey`を持つタスクのツリー項目に、そのキーが表示されること(ユーザーにより確認済み)
- [x] `jiraIssueKey`を持たないタスクの表示が、これまでと変わらないこと(ユーザーにより確認済み)
- [x] 完了状態のJira紐付けタスクで「JIRA-123 · 完了 · ファイル名」のように両方が並んで表示されること(ユーザーにより確認済み)
- [x] アンカー未接続警告(アイコン・tooltip)と共存し、崩れないこと(ユーザーにより確認済み)

## 完了条件

`requirements.md`の受け入れ条件を全て満たし、上記の実機確認が完了していること。

- [x] 実機確認完了(ユーザーにより確認済み)。このラウンドは完了とする。
